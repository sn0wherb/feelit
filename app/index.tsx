import { Redirect } from "expo-router";

const index = () => {
  // ---------------------
  // COMPONENT
  // ---------------------
  return <Redirect href="/(tabs)/feed" />;
};

export default index;
